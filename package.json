{
  "name": "wmg-quiz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test",
    "deploy:server": "cd party && npx partykit deploy"
  }
}
